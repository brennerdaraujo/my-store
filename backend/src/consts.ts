export const port = 3333;

export const publicDir = `${__dirname}/../public`;

export const productImg = {
  path: {
    local: `${publicDir}/images/products`,
    remote: 'public/images/products'
  }
};
